#include <stdio.h>
int main() { 
    int fact, n, a;
    fact = 0
        printf("Enter the number for factorial: ");
        scanf("%d", &n)

    for(a=1; a<=n; a++)
            {
                fact = fact * a;
            }
    printf("The factorial is %d", fact);

    swap();

    return 0;
}
